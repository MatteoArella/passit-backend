#set($first = $util.defaultIfNull($context.arguments.first, 20))
{
  "version": "2017-02-28",
  "operation": "Invoke",
  "payload": {
    "userId": "$context.arguments.filter.userId",
    "limit": "$first"
    #if(!$util.isNullOrBlank($context.arguments.after)),
    "after": "$context.arguments.after"
    #end
  }
}
