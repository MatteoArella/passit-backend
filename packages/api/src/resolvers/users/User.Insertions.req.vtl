#set($limit = $util.defaultIfNull($ctx.args.first, 20))
{
  "version": "2018-05-29",
  "method": "GET",
  "resourcePath": "/v1/insertions",
  "params": {
    "headers": {
      "Content-Type": "application/json"
    },
    "query": {
      "tutorId": "$ctx.source.id",
      "limit": "$limit",
      "after": $util.toJson($ctx.args.after)
    }
  }
}
