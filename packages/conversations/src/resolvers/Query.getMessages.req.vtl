#set($first = $util.defaultIfNull($context.arguments.first, 20))
{
  "version": "2017-02-28",
  "operation": "Invoke",
  "payload": {
    "conversationId": $util.toJson($context.arguments.filter.conversationId),
    "limit": "$first"
    #if(!$util.isNullOrBlank($context.arguments.after)),
    "after": $util.toJson($context.arguments.after)
    #end
  }
}
